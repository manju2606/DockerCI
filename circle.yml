deployment:
 dockerhub:
  branch: master
#   commands:
#            - $DOCKER_HUB_TRIGGER

               commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_HUB_TRIGGER

test:
  override:    
    - mocha